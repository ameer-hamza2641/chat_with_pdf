import { PDFLoader } from "@langchain";


export const getChunkedDocs=async () => {
  try {
    const loader=new PDFLoader
  } catch (error) {
    console.log('error at getting chunked docs : ', error);
  }
}